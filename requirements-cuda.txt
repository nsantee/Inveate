# ==============================================================================
# RAG Engine - GPU Acceleration (NVIDIA CUDA Only)
# Optimized for: NVIDIA Ampere Onward / x86 PC
# Requires: NVIDIA Driver & CUDA Toolkit (12.x)
# ==============================================================================

# --- PyTorch Stack (CUDA 12.6 Build) ------------------------------------------
torch==2.12.0+cu126
torchaudio==2.11.0+cu126
torchvision==0.27.0+cu126

# --- NVIDIA CUDA Libraries ----------------------------------------------------
nvidia-cuda-nvrtc-cu12==12.6.85
nvidia-cudnn-cu12==9.10.2.21
nvidia-nccl-cu12==2.29.3        # Critical for NVLink Multi-GPU
nvidia-curand-cu12==10.3.7.77
nvidia-cusolver-cu12==11.7.1.2

# --- Acceleration -------------------------------------------------------------
accelerate==1.13.0

# Note: Install these AFTER requirements-core.txt
